/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "elements-resized.envatousercontent.com","img.freepik.com"],
  },
};

export default nextConfig;
